/*
 * creation date  24 sep 2021
 * last change    24 sep 2021
 * author         artur
 */
using System;

class __MicrosoftExtensionsHostingIHostBuilder
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        MicrosoftExtensionsHostingIHostBuilder_Silent();

        Console.ReadLine();
    }
    static void MicrosoftExtensionsHostingIHostBuilder_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   MicrosoftExtensionsHostingIHostBuilder_Silent()\n");


        // Microsoft.Extensions.Hosting.IHostBuilder - общее описание ты можешь найти в методе о ASP.NET Core (если конкретно, в файле
        //   ./SimpleWeb_ASP.NET/Program.cs), где мы его затрагивали


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   MicrosoftExtensionsHostingIHostBuilder_Silent()");
    }
}