/*
 * creation date  26 jun 2021
 * last change    26 jun 2021
 * author         artur
 */
using System;

class _SystemCollectionsObjectModelReadOnlyObservableCollection
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        SystemCollectionsObjectModelReadOnlyObservableCollection_Silent();

        Console.ReadLine();
    }
    static void SystemCollectionsObjectModelReadOnlyObservableCollection_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   SystemCollectionsObjectModelReadOnlyObservableCollection_Silent()\n");


        // Класс System.Collections.ObjectModel.ReadOnlyObservableCollection<T> (не sealed) - версия ObservabelCollection, допускающая только
        //   чтение. (внезапно) Наследует ReadOnlyCollection<T>, а также реализует (из System.Collections.Specialized) INotifyCollectionChanged
        /////////after reading///////////////////////////////////////////////////////////////////////
        //   INotifyPropertyChanged
        /////////////////////////////////////////////////////////////////////////////////////////////


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   SystemCollectionsObjectModelReadOnlyObservableCollection_Silent()");
    }
}