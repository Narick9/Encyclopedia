/*
 * creation date  26 jun 2021
 * last change    26 jun 2021
 * author         artur
 */
using System;

class _SystemConvert
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        SystemConvert_Silent();

        Console.ReadLine();
    }
    static void SystemConvert_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   SystemConvert_Silent()\n");


        //****maybe you should move it to next


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   SystemConvert_Silent()");
    }
}