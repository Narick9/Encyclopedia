/*
 * creation date  02 oct 2021
 * last change    02 oct 2021
 * author         artur
 */
using System;

class __MicrosoftExtensionsDependencyInjectionIServiceCollection
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");


        MicrosoftExtensionsDependencyInjectionIServiceCollection_Silent();


        Console.ReadLine();
    }
    static void MicrosoftExtensionsDependencyInjectionIServiceCollection_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   MicrosoftExtensionsDependencyInjectionIServiceCollection_Silent()\n");


        // Microsoft.Extensions.DependencyInjection.IServiceCollection - общее описание ты можешь найти в методе о ASP.NET Core (если точнее,
        //   в файле ./SimpleWeb_ASP.NET/Startup.cs), где этот интерфес применялся впервые
        //
        // Интерфейс определён в Microsoft.Extensions.DependencyInjection.Abstractions.dll


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   _Silent()");
    }
}