/*
 * creation date  19 oct 2021
 * last change    19 oct 2021
 * author         artur
 */
using System;

class __MicrosoftAspNetCoreHostingIWebHostEnvironment
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        MicrosoftAspNetCoreHostingIWebHostEnvironment_Silent();

        Console.ReadLine();
    }
    static void MicrosoftAspNetCoreHostingIWebHostEnvironment_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   MicrosoftAspNetCoreHostingIWebHostEnvironment_Silent()\n");


        // Microsoft.AspNetCore.Hosting.IWebHostEnvironment --- общее описание ты можешь найти в методе о ASP.NET Core (если точнее,
        //   в файле ./SimpleWeb_ASP.NET/Startup.cs), где этот интерфес ты встретил впервые
        //
        // Сам интерфейс определён в Microsoft.AspNetCore.Hosting.Abstractions.dll


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   MicrosoftAspNetCoreHostingIWebHostEnvironment_Silent()");
    }
}