/*
 * creation date  20 jan 2021
 * last change    26 jun 2021
 * author         artur
 */
using System;
using System.Collections.Generic;

class _SystemCollectionsSortedList
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        SystemCollectionsSortedList_Silent();

        Console.ReadLine();
    }
    static void SystemCollectionsSortedList_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   SystemCollectionsSortedList_Silent()\n");


        // Класс System.Collections.SortedList (снова незапечатан) - ещё одна коллекция пар ключ-значение, но отсрортированная по ключу и с
        //   доступом по ключу и по индексу. Реализует (из System.Collections) IDictionary, ICollections, IEnumerable и (из System) ICloneable


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   SystemCollectionsSortedList_Silent()");
    }
}