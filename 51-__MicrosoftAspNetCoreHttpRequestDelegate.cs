/*
 * creation date  19 oct 2021
 * last change    19 oct 2021
 * author         artur
 */
using System;

class __MicrosoftAspNetCoreHttpRequestDelegate
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        MicrosoftAspNetCoreHttpRequestDelegate_Silent();

        Console.ReadLine();
    }
    static void MicrosoftAspNetCoreHttpRequestDelegate_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   MicrosoftAspNetCoreHttpRequestDelegate_Silent()\n");


        // Microsoft.AspNetCore.Http.RequestDelegate - общее описание ты можешь найти в методе о ASP.NET Core (если точнее,
        //   в файле ./SimpleWeb_ASP.NET/Startup.cs), где этот делегат был использован впервые
        //   
        //   
        // Интерфейс находится в Microsoft.AspNetCore.Http.Abstractions.dll


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   MicrosoftAspNetCoreHttpRequestDelegate_Silent()");
    }
}