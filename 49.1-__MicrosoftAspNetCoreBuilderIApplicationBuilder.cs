/*
 * creation date  19 oct 2021
 * last change    19 oct 2021
 * author         artur
 */
using System;

class __MicrosoftAspNetCoreBuilderIApplicationBuilder
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        MicrosoftAspNetCoreBuilderIApplicationBuilder_Silent();

        Console.ReadLine();
    }
    static void MicrosoftAspNetCoreBuilderIApplicationBuilder_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   MicrosoftAspNetCoreBuilderIApplicationBuilder_Silent()\n");


        // Microsoft.AspNetCore.Builder.IApplicationBuilder --- общее описание ты можешь найти в методе о ASP.NET Core (если точнее,
        //   в файле ./SimpleWeb_ASP.NET/Startup.cs), где этот интерфес ты встретил впервые
        //
        // Сам интерфейс определён в Microsoft.AspNetCore.Http.Abstractions.dll


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   MicrosoftAspNetCoreBuilderIApplicationBuilder_Silent()");
    }
}