/*
 * creation date  22 aug 2021
 * last change    22 aug 2021
 * author         artur
 */
using System;

class _CRUD
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        CRUD_Silent();

        Console.ReadLine();
    }
    static void CRUD_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   CRUD_Silent()\n");


        // CRUD - Create, Read, Update, Delete
        //****это должно быть в методе, где объясняться работа с файл система


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   CRUD_Silent()");
    }
}