/*
 * creation date  26 jun 2021
 * last change    26 jun 2021
 * author         artur
 */
using System;

class _SystemCollectionsGenericSortedDictionary
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        SystemCollectionsGenericSortedDictionary_Silent();

        Console.ReadLine();
    }
    static void SystemCollectionsGenericSortedDictionary_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   SystemCollectionsGenericSortedDictionary_Silent()\n");


        // Класс System.Collections.Generic.SortedDictionary<T,Y> (не sealed) - обобщённая коллекция сортированных пар key-value. Реализует (из
        //   System.Collections.Generic) IDictionary<T,Y>, ICollection<KeyValuePair<T,Y>>, IEnumerable<KeyValuePair<T,Y>>,
        //   IReadOnlyDictionary<T,Y>, IReadOnlyCollection<KeyValuePair<T,Y>>, (из System.Collections) IEnumerable, IDictionary, ICollection


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   SystemCollectionsGenericSortedDictionary_Silent()");
    }
}