/*
 * creation date  02 oct 2021
 * last change    02 oct 2021
 * author         artur
 */
using System;

class __MicrosoftAspNetCoreRoutingIEndpointRouteBuilder
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");


        MicrosoftAspNetCoreRoutingIEndpointRouteBuilder_Silent();


        Console.ReadLine();
    }
    static void MicrosoftAspNetCoreRoutingIEndpointRouteBuilder_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   MicrosoftAspNetCoreRoutingIEndpointRouteBuilder_Silent()\n");


        // Microsoft.AspNetCore.Routing.IEndpointRouteBuilder - общее описание ты можешь найти в методе о ASP.NET Core (если точнее,
        //   в файле ./SimpleWeb_ASP.NET/Startup.cs), где этот интерфейс был встречен впервые
        //   
        // Интерфейс этот немалый, среднего размера
        //   
        // Определён он в Microsoft.AspNetCore.Routing.dll


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   _Silent()");
    }
}