/*
 * creation date  26 jun 2021
 * last change    26 jun 2021
 * author         artur
 */
using System;

class _SystemCollectionsBitArray
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        SystemCollectionsBitArray_Silent();

        Console.ReadLine();
    }
    static void SystemCollectionsBitArray_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   SystemCollectionsBitArray_Silent()\n");


        // Запечатанный класс System.Collections.BitArray управляет компактным битовым массивом. Класс реализует (из System.Collections)
        //   ICollections, IEnumerable и (из System) ICloneable


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   SystemCollectionsBitArrya_Silent()");
    }
}