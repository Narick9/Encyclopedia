/*
 * creation date  02 oct 2021
 * last change    02 oct 2021
 * author         artur
 */
using System;

class __SecretManager
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");


        SecretManager_Silent();


        Console.ReadLine();
    }
    static void SecretManager_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   SecretManager_Silent()\n");


        // Secret Manager - впервые был упомянут в методе о ASP.NET Core (если точнее, в файле ./SimpleWeb_ASP.NET/Program.cs), где ты и
        //   сможешь найти краткое описание этой штуки


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   SecretManager_Silent()");
    }
}