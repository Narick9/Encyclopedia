/*
 * creation date  26 jun 2021
 * last change    26 jun 2021
 * author         artur
 */
using System;

class _SystemCollectionsObjectModelCollection
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        SystemCollectionsObjectModelCollection_Silent();

        Console.ReadLine();
    }
    static void SystemCollectionsObjectModelCollection_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   SystemCollectionsObjectModelCollection_Silent()\n");


        // Класс System.Collections.ObjectModel.Collection<T> (не sealed) - призван стать базовым классом твоей будущей коллекции. Сам по себе
        //   реализует (из System.Collections.Generic) IList<T>, ICollection<T>, IEnumerable<T>, IReadOnlyList<T>, IReadOnlyCollection<T>, (из
        //   System.Collections) IEnumerable, IList, ICollection


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   SystemCollectionsObjectModelCollection_Silent()");
    }
}