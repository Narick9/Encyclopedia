/*
 * creation date  01 aug 2021
 * last change    01 aug 2021
 * author         artur
 */
using System;

class _SystemTextEncoding
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        SystemTextEncoding_Silent();

        Console.ReadLine();
    }
    static void SystemTextEncoding_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   SystemTextEncoding_Silent()\n");


        // System.Text.Encoding - класс, что представляет кодировки.
        //   Некоторые самые популярные можно получить прямо из его
        //   статических свойств


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   SystemTextEncoding_Silent()");
    }
}