/*
 * creation date  24 jan 2021
 * last change    26 jun 2021
 * author         artur
 */
using System;
using System.Collections.Generic;

class SystemCollectionsGenericIReadOnlyList
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        SystemCollectionsGenericIReadOnlyList_Silent();

        Console.ReadLine();
    }
    static void SystemCollectionsGenericIReadOnlyList_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   SystemCollectionsGenericIReadOnlyList_Silent()\n");


        // 


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   SystemCollectionsGenericIReadOnlyList_Silent()");
    }
}