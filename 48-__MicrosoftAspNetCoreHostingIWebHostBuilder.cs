/*
 * creation date  28 sep 2021
 * last change    28 sep 2021
 * author         artur
 */
using System;

class __MicrosoftAspNetCoreHostingIWebHostBuilder
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        MicrosoftAspNetCoreHostingIWebHostBuilder_Silent();

        Console.ReadLine();
    }
    static void MicrosoftAspNetCoreHostingIWebHostBuilder_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   MicrosoftAspNetCoreHostingIWebHostBuilder_Silent()\n");


        // Microsoft.AspNetCore.Hosting.IWebHostBuilder - общее описание ты можешь найти в методе о ASP.NET Core (если точнее, в файле
        //   ./SimpleWeb_ASP.NET/Program.cs), где этот интерфейс мы встретились с этим интерфейсом
        //   (****чем web host отличается от host?)


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   MicrosoftAspNetCoreHostingIWebHostBuilder_Silent()");
    }
}