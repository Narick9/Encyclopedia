/*
 * creation date  24 sep 2021
 * last change    24 sep 2021
 * author         artur
 */
using System;

class __MicrosoftExtensionsHostingHost
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        MicrosoftExtensionsHostingHost_Silent();

        Console.ReadLine();
    }
    static void MicrosoftExtensionsHostingHost_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   MicrosoftExtensionsHostingHost_Silent()\n");


        // Microsoft.Extensions.Hosting.Host - общее описание ты можешь найти в методе о ASP.NET Core (если точнее, в файле
        //   ./SimpleWeb_ASP.NET/Program.cs), где этот static класс был задейстован впервые
        //
        // Если вкратце, этот static класс всего-навсего поставляет пару удобных методов для создания объектов builder'а
        //   Microsoft.Exte...IHostBuilder


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   MicrosoftExtensionsHostingHost_Silent()");
    }
}