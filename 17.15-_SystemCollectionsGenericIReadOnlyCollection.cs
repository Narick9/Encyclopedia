/*
 * creation date  26 jun 2021
 * last change    26 jun 2021
 * author         artur
 */
using System;

class _SystemCollectionsGenericIReadOnlyCollection
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        SystemCollectionsGenericIReadOnlyCollection_Silent();

        Console.ReadLine();
    }
    static void SystemCollectionsGenericIReadOnlyCollection_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   void()\n");


        // 


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   void()");
    }
}