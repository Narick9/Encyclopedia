/*
 * creation date  21 aug 2021
 * last change    21 aug 2021
 * author         artur
 */
using System;

class __
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");


        Console.ReadLine();
    }
    static void _Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   _Silent()\n");


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   _Silent()");
    }
}