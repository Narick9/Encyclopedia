/*
 * creation date  26 jun 2021
 * last change    26 jun 2021
 * author         artur
 */
using System;

class _SystemCollectionsIDictionary
{
    static void Main()
    {
        Console.WriteLine("***** _ *****");

        SystemCollectionsIDictionary_Silent();

        Console.ReadLine();
    }
    static void SystemCollectionsIDictionary_Silent()
    {
        Console.WriteLine(">->->->->->->->->->->->->->->->->->->   SystemCollectionsIDictionary_Silent()\n");


        // Интерфейс System.Collections.IDictionary гарантирует некоторые члены для предоставления пар вида ключ-значение. Наследует
        //   System.Collections.ICollection


        Console.WriteLine("<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<   SystemCollectionsIDictionary_Silent()");
    }
}